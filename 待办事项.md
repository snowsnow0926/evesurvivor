# 待办事项

> 创建时间：2026-05-06
> 最后更新：2026-05-06（根据代码全面分析报告 v1.0 生成修复清单）

---

## 菜单界面清理（MainMenu.tscn） ~~(已完成)~~

> 场景：`res://scenes/MainMenu.tscn`，以下节点已不需要，标记待移除

| # | 节点名称 | 说明 |
|---|---------|------|
| 1 | `CoinLabel` | 不需要了 |
| 2 | `MineralsLabel` | 不需要了 |
| 3 | `ShipStatus` | 不需要了 |
| 4 | `RepairsBtn` | 不需要了 |

---

## 🔴 P0 — 必须立即修复（影响核心玩法）

| # | Bug | 文件 | 问题描述 | 影响 | 状态 |
|---|-----|------|---------|------|------|
| B1 | `apply_upgrade` 双重调用 | `upgrade_menu.gd` | `game_manager.apply_upgrade()` 被 `_on_btn_pressed()` 和 `confirm_selection()` 各调用一次 | 升级效果翻倍（加农炮+1变+2、伤害+20%变+40%） | ~~待修复~~ 已完成 |
| B2 | Boss 可能无限刷新 | `spawn_manager.gd` 第 159-166 行 | `kill_since_boss >= 50` 条件未包含在 `if boss_remaining > 0` 内部；`_spawn_boss` 缺少 `boss_active` 守卫 | Boss 战可能无限循环，破坏游戏流程 | ~~待修复~~ 已完成 |
| B3 | `is_boss_phase` 永远为 false | `game_manager.gd` 第 298 行 | `game_manager` 的 `is_boss_phase` 从未被赋值，应读 `spawn_manager.is_boss_phase` | Boss phase 相关逻辑永不触发 | ~~待修复~~ 已完成 |
| B4 | Boss 死亡不掉落装备 | `boss_void.gd` 第 140-146 行 | `_die()` 覆盖漏掉了 `try_drop_equipment()` 调用 | Boss 战利品只有货币+矿物，装备不掉落 | ~~待修复~~ 已完成 |

---

## 🟠 P1 — 重要（影响用户体验）

| # | 问题 | 文件 | 问题描述 | 影响 | 状态 |
|---|------|------|---------|------|------|
| P1-1 | 所有 SFX 路径为空 | `sound_manager.gd` 第 3-21 行 | `SFX_RESOURCES` 字典所有值都是空字符串 `""` | 游戏完全无音效 | 待修复 |
| P1-2 | 结算画面频繁 print | `settlement_scene.gd` 多处 | 第 75/78/101 行等大量 print 语句，且 `_gui_input` 每帧打印所有输入事件 | 严重影响性能 | 待修复 |
| P1-3 | 经验球每生成打印一次 | `exp_orb.gd` 第 20 行 | 每创建一个 orb 打印一次，大量敌人死亡时刷屏 | 严重影响性能 | 待修复 |
| P1-4 | 商店品质全为 0 | `shop_data.gd` | 全部 24 个物品 `quality = 0`，商店永远不显示高 rarity 物品 | 与战利品掉落品质系统完全脱节 | 待修复 |
| P1-5 | Boss 死亡音效播放 3 次 | `enemy_raven.gd` + `enemy_base.gd` | `_do_explosion()` 播放1次 + `enemy_base._die()` 播放1次 + `enemy_raven._die()` 又播放1次 | 体验问题，音效刺耳 | 待修复 |

---

## 🟡 P2 — 建议（代码质量 / 维护性）

| # | 问题 | 文件 | 说明 | 状态 |
|---|------|------|------|------|
| P2-1 | 武器默认值重复定义 | `game_manager.gd` 第 135-150 行 + `player.gd` 第 179-194 行 | 两处必须同步，维护负担大 | 待处理 |
| P2-2 | `game_balance.json` 配置被硬编码 | `spawn_manager.gd` 第 162、165 行 | JSON 中的 boss 击杀阈值不生效 | 待处理 |
| P2-3 | 敌人速度计算基准不一致 | `difficulty_scaler.gd` vs `spawn_manager.gd` | 一个用 `stage_strength`，一个用 `final_strength`，系数相同但基准不同 | 待处理 |
| P2-4 | `spawn_interval` 各算各的 | `difficulty_scaler.gd` vs `spawn_manager.gd` | difficulty_scaler 计算的 spawn_interval 从未被使用 | 待处理 |
| P2-5 | `_show_settlement_screen()` 过长 | `game_scene.gd` | 超过 160 行，职责过多 | 待处理 |
| P2-6 | 25 项无用代码清理 | 多个文件（见下表） | 删除无用代码，改善代码整洁度 | 待处理 |
| P2-7 | `boss_bullet.gd` / `sentry_bullet.gd` 合并 | 两个文件 97% 相同 | 违反 DRY 原则，应合并为 `EnemyBullet.gd` | 待处理 |
| P2-8 | `difficulty_scaler.gd` 空 `_init()` | `difficulty_scaler.gd` 第 18-19 行 | 不执行任何操作，直接删除 | 待处理 |
| P2-9 | `player.gd` 空 `reset_state()` / `_input()` | `player.gd` 第 665/668 行 | 不执行任何操作 | 待处理 |
| P2-10 | `upgrade_system.gd` 死参数 | `upgrade_system.gd` 第 56-57 行 | 两个参数 `_` 声明但完全不使用 | 待处理 |

### P2-6 无用代码详细清单（25 项）

| # | 文件 | 无用代码 | 说明 | 建议 |
|---|------|---------|------|------|
| 1 | `game_state.gd` | `SAVE_PATH` 常量 | 从未使用 | 删除 |
| 2 | `game_state.gd` | `_pending_save` / `_pending_slot` | 永远不被设置，死代码 | 删除或实现 |
| 3 | `sound_manager.gd` | `_get_property_list()` | 在 Godot 4 中无效 | 改用 `@export` 或移除 |
| 4 | `player.gd` | `get_talent_bonus()` | 无人调用 | 删除或连接使用 |
| 5 | `enemy_melee.gd` | `_physics_process` 覆盖 | 完全重复父类 | 删除 |
| 6 | `enemy_sentry.gd` | `_physics_process` 覆盖 | 完全重复父类 | 删除 |
| 7 | `enemy_raven.gd` | `_physics_process` 覆盖 | 完全重复父类 | 删除 |
| 8 | `enemy_base.gd` | `$Polygon2D` 节点 | 全程 `visible = false`，纯占位 | 删除节点和脚本引用 |
| 9 | `enemy_base.gd` | `_chapter_tonnage` 变量 | 赋值但从未读取 | 删除 |
| 10 | `enemy_raven.gd` | `_process_combat` 空 `pass` | 与其他子类设计不一致 | 合并或删除 |
| 11 | `enemy_raven.gd` | `_death_particle_*` 赋值 | 赋值后立刻 `queue_free()` 不被使用 | 删除赋值语句 |
| 12 | `enemy_sentry.gd` | `_die()` 中的 `"windup"` 检查 | sentry 不使用 windup | 删除检查 |
| 13 | `boss_void.gd` | `hp_bar_bg` 变量 | 获取但从未使用 | 删除 |
| 14 | `damage_number.gd` | `_ready` 空函数 | 不执行任何操作 | 删除 |
| 15 | `hud.gd` | 第 320 行空 `pass` | 无操作 | 删除 |
| 16 | `settlement_scene.gd` | `_ready` 中 `_data_ready` 条件分支 | 永远不执行，死代码 | 删除 |
| 17 | `upgrade_system.gd` | `trigger_upgrade_request` 的两个死参数 | 完全不使用 | 删除参数列表 |
| 18 | `game_scene.gd` | `_on_upgrade_selected` 空函数 | 不执行任何操作 | 删除 |
| 19 | `warehouse_ui.gd` | `equip_detail` 动态 `find_child` | 风格不一致 | 改用 `@onready` |
| 20 | `game_state.gd` | `reset_all_data()` 中的 `pre_run_*` 赋值 | 调试函数重置后赋值，无意义 | 确认是否需要 |
| 21 | `race_select.gd` | 整个文件 | 可能是废弃流程（如果 `character_create.gd` 已替代） | 确认是否还在使用后删除 |

---

## 🟢 P3 — 未来优化（可选）

| # | 问题 | 文件 | 说明 | 状态 |
|---|------|------|------|------|
| P3-1 | `virtual_joystick` / `mobile_input` 未集成 | — | 移动端支持，代码已完整实现 | 未来规划 |
| P3-2 | `ship_storage.gd` 功能未实现 | `ship_storage.gd` | 装备配置下版本计划 | 未来规划 |
| P3-3 | `newbie_guide.gd` 信号连接不完整 | `newbie_guide.gd` | `notify_enemy_killed()` / `notify_level_up()` 是空函数 | 未来规划 |
| P3-4 | `race_select.gd` 可能是废弃流程 | `race_select.gd` | `character_create.gd` 已替代旧流程 | 确认后删除 |

---

## 玩家舰船护盾值调整 ~~(已完成)~~

| 舰船 | 基础护盾 |
|------|---------|
| 护卫舰 | **50** |
| 巡洋舰 | **100** |
| 战列巡洋舰 | **200** |
| 战列舰 | **350** |
| 无畏舰 | **600** |
| 泰坦 | **1,200** |

---

## 角色面板美术素材需求

| # | 内容 | 说明 |
|---|-----|------|
| 1 | 删除 "V0.8" 字样 | 版本号标签移除 |
| 2 | 玩家飞船图片 | 角色面板需展示玩家飞船美术素材 |
| 3 | 驾驶员立绘图 | 角色面板需展示驾驶员立绘美术素材 |

---

## 其他待处理问题

| # | 问题 | 说明 | 状态 |
|---|------|------|------|
| 1 | 掉落物品显示问号 | 检查掉落物品显示问号的情况 | 待调查 |
| 2 | 存档界面"最高等级"显示 | 读取游戏时应显示"当前解锁最高关卡进度"而非"最高等级" | ~~待修复~~ 已完成 |

